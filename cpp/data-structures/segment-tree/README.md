# Segment Tree

A **binary** tree built over an array where each node stores an aggregated result (sum, min, max) for a contiguous subarray (or segment).

Good for:
- Range queries (e.g. sum from index 2 to 7) --> O(log N)
- Performing point updates --> O(log N)
